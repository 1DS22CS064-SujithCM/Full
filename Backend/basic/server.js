const app=require("./src/app")

app.listen(3000,()=>{
  console.log("server is running in the port 3000")
})    // starting/calling the server and () is the callback and it is successfully executed when the calling is done